{
  "name": "projectflow-playwright-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone Node/TypeScript worker that executes SC/TS rule checks with Playwright and returns JSON results. Invoked by Laravel as a child process (stdin JSON in, stdout JSON out) -- never exposed as a network service.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "run": "node dist/index.js"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
